[package]
name = "auction-guaranteed"
version = "1.0.0"
edition = "2018"

[lib]
crate-type = ["cdylib"]
name = "auction_guaranteed"  # Explicit library name

[dependencies]
cosmwasm-std = "2.3.2"
serde = { version = "1", features = ["derive"] }
cw-storage-plus = "2.0"
cosmwasm-schema = "3.0"
schemars = "0.8"
thiserror = "2.0"

[dev-dependencies]
proptest = "1.4"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
